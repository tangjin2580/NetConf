# Python 3.8+ 兼容版本依赖 (支持 Windows 7)
# 安装命令: pip install -r requirements.txt

# 核心依赖 - 使用兼容Win7的版本
Pillow>=9.0.0,<10.0.0
requests>=2.28.0,<3.0.0

# PyInstaller - 5.13是最后一个稳定支持Win7的版本
pyinstaller>=5.13.0,<6.0.0
